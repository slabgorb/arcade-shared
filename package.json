{
  "name": "@arcade/shared",
  "version": "0.1.0",
  "description": "Shared library for the slabgorb browser vector-arcade games (ADR-0001). Consumed as a version-pinned git-URL dependency.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slabgorb/arcade-shared.git"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "prepare": "tsc -p tsconfig.build.json",
    "test": "echo \"(no tests yet — the first suite lands with the math3d extraction in SH-2)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
